# Local dev default for the API origin. Committed on purpose: it is not a
# secret, it keeps the warm-up ping in index.html from warning on every request
# that %VITE_API_URL% is undefined, and it documents what `npm run dev:server`
# serves. Production values are set in the Vercel project, not here.
VITE_API_URL=http://localhost:4000
